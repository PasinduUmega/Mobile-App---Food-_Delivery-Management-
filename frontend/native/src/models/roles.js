/**
 * Mirrors `UserRole` from `lib/models.dart` — API/backend use UPPERCASE strings.
 * @readonly
 */
export const UserRole = Object.freeze({
  CUSTOMER: 'CUSTOMER',
  ADMIN: 'ADMIN',
  STORE_OWNER: 'STORE_OWNER',
  DELIVERY_DRIVER: 'DELIVERY_DRIVER',
});
