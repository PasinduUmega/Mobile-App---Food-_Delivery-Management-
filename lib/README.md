Dart source for the Flutter (mobile/desktop/web) client. **`pubspec.yaml` expects this folder.**

JavaScript equivalents for services, UI stubs, and config live under **`frontend/native/src/`**, parallel to `services/`, `ui/`, `models/`, `config.dart`.  

See **`frontend/FLUTTER_LIB_MAP.md`**.
